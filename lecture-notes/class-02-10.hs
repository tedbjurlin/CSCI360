-- 02/10

-- Syntax

-- "2 + 34 * 5" (concrete syntax) ——> Parsing ——> (abstract syntax)

-- Abstract syntax (tree) (AST) structured representation of the language syntax

-- (abstract syntax) ——> Pretty Printing ——> (concrete syntax)

-- can be concrete to parsing to abstract or concrete to lexing to sequence of 
--  tokens to parsing to abstract

-- abstract syntax can be optimized or desugared

-- Pretty printing is used in printing error messages so that the user can
--  understand them 